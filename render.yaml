# Render Blueprint: https://render.com/docs/blueprint-spec
# Connect this repo in the Render dashboard, or use "Apply" on the Blueprint.
services:
  - type: web
    name: lang-detect
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
